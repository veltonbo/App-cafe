import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{CloudRain,Droplets,Gauge,History,Settings2,ShieldCheck,Sparkles,Thermometer,TimerReset,Wifi,Activity,BrainCircuit,Server,TriangleAlert}from'lucide-react';import{irrigationApi}from'./api';import'./styles.css';
type D=Record<string,any>;const f=(v:any,s='—')=>v==null||v===''?s:String(v);const tabs=['Resumo','Automação','Histórico','Sistema'];function App(){const[d,setD]=useState<D>({}),[diag,setDiag]=useState<D>({}),[tab,setTab]=useState('Resumo'),[err,setErr]=useState(true);useEffect(()=>{let live=true;const load=()=>irrigationApi.dashboard().then(x=>{if(live){setD(x);setErr(false)}}).catch(()=>live&&setErr(true));load();const t=setInterval(load,5000);return()=>{live=false;clearInterval(t)}},[]);useEffect(()=>{if(tab==='Sistema')irrigationApi.diagnostics().then(setDiag).catch(()=>{})},[tab]);return <main><Header err={err}/>{err&&<p role="status">Conexão não confirmada. Para conectar ou controlar a irrigação, <a href="/irrigacao/">abra o painel principal</a>.</p>}{tab==='Resumo'?<Summary d={d}/>:tab==='Automação'?<Automation d={d}/>:tab==='Histórico'?<HistoryView d={d}/>:<System d={d} diag={diag}/>}<Nav tab={tab} setTab={setTab}/></main>}
function Header({err}:any){return <header><div><small>FAZENDA 2E • VIVEIRO</small><h1>Irrigação inteligente</h1></div><span className={err?'offline':'online'}><Wifi size={13}/>{err?' Reconectando':' Online'}</span></header>}
function Summary({d}:any){const w=d.current_weather?.metrics||{},a=d.climate?.shadow4||{},s=d.seconds||d.fast_cycle||{},state=f(s.state||d.status,'AGUARDANDO');return <><section className="hero"><div><small>ESTADO OPERACIONAL</small><h2>{state.toUpperCase()}</h2><p>{a.reason||'Monitoramento contínuo do viveiro e das proteções.'}</p></div><div className="heroCycle"><span>CICLO</span><b>{f(s.on_seconds||a.current_on_seconds)}/{f(s.off_seconds||a.current_off_seconds)}s</b></div></section><section className="metrics"><Card icon={<Thermometer/>} label="Temperatura" value={`${f(w.temperature?.value)}°C`}/><Card icon={<Droplets/>} label="Umidade" value={`${f(w.humidity?.value)}%`}/><Card icon={<Gauge/>} label="VPD" value={`${f(d.climate?.state?.last_vpd??d.climate?.state?.vpd)} kPa`}/><Card icon={<CloudRain/>} label="Chuva" value={w.rainDetected?'Detectada':`${f(w.rain24h?.value??w.rainGeneric?.value)} mm`}/></section><Decision a={a}/><section className="panel"><Title over="ANÁLISE RÁPIDA" title="Desempenho de hoje" icon={<Activity/>}/><div className="grid4"><Mini label="Pulsos" value={f(d.summary?.today?.pulses)}/><Mini label="Tempo irrigado" value={f(d.summary?.today?.irrigated_seconds==null?null:d.summary.today.irrigated_seconds+' s')}/><Mini label="Incidentes" value={f(d.reports?.incidents?.totals?.open)}/><Mini label="Smart Life" value={d.current_weather?.provider==='smartlife'&&d.current_weather?.ok===true?'Respondendo':'Sem confirmação'}/></div></section></>}
function Decision({a}:any){return <section className="decision"><Title over="AUTOMÁTICO 4.0 • SOMBRA" title="Central de decisão" icon={<BrainCircuit/>}/><div className="decisionMain"><b>{f(a.decision,'OBSERVANDO')}</b><span>Confiança {f(a.confidence_label)} {a.confidence_score!=null?`• ${a.confidence_score}%`:''}</span></div><p>{a.reason||'Coletando dados para comparar as decisões do novo automático.'}</p><div className="grid4"><Mini label="Atual" value={a.current_on_seconds?`${a.current_on_seconds}/${a.current_off_seconds}s`:'—'}/><Mini label="4.0 faria" value={a.target_on_seconds?`${a.target_on_seconds}/${a.target_off_seconds}s`:'—'}/><Mini label="Demanda" value={a.demand_percent!=null?`${a.demand_percent>0?'+':''}${a.demand_percent}%`:'—'}/><Mini label="Segurança" value="5 camadas"/></div><div className="chips">{(a.factors||[]).slice(0,5).map((x:string)=><span key={x}>{x}</span>)}</div></section>}
function Automation({d}:any){const a=d.climate?.shadow4||{},s=d.seconds||d.fast_cycle||{};return <><PageTitle title="Automação" text="Controle, inteligência e segurança em uma única visão."/><Decision a={a}/><section className="panel"><Title over="CICLO OPERACIONAL" title="Programação atual" icon={<TimerReset/>}/><div className="grid4"><Mini label="Ligado" value={`${f(s.on_seconds)} s`}/><Mini label="Desligado" value={`${f(s.off_seconds)} s`}/><Mini label="Início" value={f(s.start_time||s.start)}/><Mini label="Fim" value={f(s.end_time||s.end)}/></div></section><section className="panel"><Title over="BARREIRAS INDEPENDENTES" title="Proteções" icon={<ShieldCheck/>}/><p className="muted">Horário permitido, chuva, watchdog, limite máximo ligado e falha de comunicação permanecem acima do Automático 4.0.</p></section></>}
function HistoryView({d}:any){const x=d.analytics||{};return <><PageTitle title="Histórico analítico" text="Informações para entender o que aconteceu e melhorar as próximas decisões."/><section className="panel"><Title over="HOJE" title="Resumo operacional" icon={<History/>}/><div className="grid4"><Mini label="Ciclos" value={f(x.today?.cycles||x.today?.pulses)}/><Mini label="Conclusão" value={f(x.today?.completion_rate)}/><Mini label="Pausas chuva" value={f(x.today?.rain_pauses)}/><Mini label="Falhas" value={f(x.today?.failures||0)}/></div></section><section className="panel"><Title over="COMPARAÇÃO" title="7 e 30 dias" icon={<Gauge/>}/><div className="compare"><Mini label="7 dias • irrigação" value={f(x.week?.irrigated_time)}/><Mini label="30 dias • irrigação" value={f(x.month?.irrigated_time)}/></div><p className="muted">A estrutura está preparada para receber séries climáticas, pulsos planejados × realizados e comparativos por período.</p></section></>}
function System({d,diag}:any){return <><PageTitle title="Sistema" text="Saúde do servidor e comunicação sem misturar detalhes técnicos ao painel principal."/><section className="panel"><Title over="INFRAESTRUTURA" title="Saúde do Fazenda 2E" icon={<Server/>}/><div className="grid4"><Mini label="Servidor" value={diag.ok===true?'Online':diag.ok===false?'Atenção':'Sem confirmação'}/><Mini label="Memória" value={f(diag.memory?.process||diag.process_memory)}/><Mini label="Backups" value={f(diag.backups?.count)}/><Mini label="Histórico local" value={f(diag.local_history?.records)}/></div></section><section className="panel"><Title over="COMUNICAÇÃO" title="Smart Life" icon={<Wifi/>}/><div className="grid4"><Mini label="EKAZA" value={f(d.devices?.ekaza?.status||d.ekaza?.status,'Monitorando')}/><Mini label="Weather2-2" value={f(d.devices?.weather?.status||d.weather?.status,'Monitorando')}/><Mini label="Tempo real" value="Consultar painel principal"/><Mini label="Proteção" value="Consultar painel principal"/></div></section></>}
function PageTitle({title,text}:any){return <div className="pageTitle"><h2>{title}</h2><p>{text}</p></div>}function Title({over,title,icon}:any){return <div className="title"><div><small>{over}</small><h3>{title}</h3></div>{icon}</div>}function Card({icon,label,value}:any){return <div className="card"><span>{icon}</span><small>{label}</small><b>{value}</b></div>}function Mini({label,value}:any){return <div className="mini"><small>{label}</small><b>{value}</b></div>}function Nav({tab,setTab}:any){const icons=[<Gauge/>,<TimerReset/>,<History/>,<Settings2/>];return <nav>{tabs.map((n,i)=><button className={tab===n?'active':''} onClick={()=>setTab(n)} key={n}>{icons[i]}<span>{n}</span></button>)}</nav>}createRoot(document.getElementById('root')!).render(<App/>);